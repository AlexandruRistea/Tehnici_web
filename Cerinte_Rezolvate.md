# Cerinte Rezolvate

## Etapa 1

- `index.html` + doctype + limba documentului: pagina principala exista, documentul porneste cu `<!DOCTYPE html>` si foloseste `<html lang="ro">` in `index.html` (line 1).
- `title` si meta-uri: titlul paginii, `charset`, `author`, `keywords`, `description`, `viewport`, `theme-color` si `msapplication-TileColor` sunt in `index.html` (line 4).
- Cuvinte cheie in continut: expresiile relevante pentru tema apar direct in zona de prezentare in `index.html` (line 63).
- Folder `resurse` si favicon: fisierele de favicon sunt legate in head in `index.html` (line 18), iar fisierele exista in `resurse/ico/favicon.svg` (line 1) si `resurse/ico/site.webmanifest` (line 1).
- Impartire semantica `header`, `main`, `footer`: structura principala este in `index.html` (line 26), `index.html` (line 53) si `index.html` (line 352).
- Taguri de sectionare si imbricare: sunt folosite `section` si `aside`, inclusiv sectiuni imbricate, in `index.html` (line 54), `index.html` (line 221) si `index.html` (line 233).
- `hgroup`: titlul si subtitlul zonei de prezentare sunt grupate in `index.html` (line 55).
- Navigare in `header`: meniul cu suboptiuni pentru `Acasa` este in `index.html` (line 32).
- Taguri de grupare: `p`, `blockquote` si `dl` apar in `index.html` (line 62), `index.html` (line 249) si `index.html` (line 241).
- Sectiune de evenimente cu `time`: calendarul are elemente cu `time` si `datetime` in `index.html` (line 176).
- Numele evenimentelor marcate vizual: evenimentele folosesc `b` in `index.html` (line 180).
- Imagine cu `figure`, `figcaption`, `picture`: imaginea principala este implementata in `index.html` (line 80).
- Variatii de imagine pentru ecrane diferite: sursele responsive sunt in `index.html` (line 82).
- Cerinte de text special: `b`, `i`, `strong`, `em`, `s`, `ins`, `abbr`, `dfn`, `cite`, `q` apar in `index.html` (line 63), `index.html` (line 69), `index.html` (line 96) si `index.html` (line 100).
- Cele 5 tipuri de linkuri: link extern in `index.html` (line 226), link cu fragment extern in `index.html` (line 226), link spre inceputul paginii in `index.html` (line 370), link care contine o imagine in `index.html` (line 106), link `download` in `index.html` (line 109).
- `iframe` cu YouTube si linkuri care se deschid in acelasi frame: implementarea este in `index.html` (line 254).
- Tabel complet cu `caption`, `thead`, `tbody`, `tfoot`, `rowspan`, `colspan`: structura tabelului este in `index.html` (line 116).
- `details` si `summary`: sectiunea FAQ este in `index.html` (line 275).
- `meter`: cele doua contoare sunt in `index.html` (line 205).
- `address` si date de contact: telefon `tel:`, Google Maps, email `mailto:` si WhatsApp sunt in `index.html` (line 353).
- Copyright cu `small`, `&copy;` si `time`: implementarea este in `index.html` (line 371).
- Harta de imagine cu `map` si `area`: este in `index.html` (line 89).
- PDF afisat in pagina prin `object`: este in `index.html` (line 321).
- Iframe Google Maps in pagina: este in `index.html` (line 362).
- Playlist YouTube cu autoplay, loop si playlist: este in `index.html` (line 331).

## Etapa 2

- Repository local Git si lucrul pe branch-ul etapei: repo-ul este pe branch-ul `Etapa_2` in `.git/HEAD` (line 1).
- Schema cromatica individuala si variabile CSS: paleta `#38378C #498C37 #8C5537 #282837 #E9FFE3` este definita prin variabile in `resurse/css/stil.css` (line 1).
- Layout responsive individual cu 8 zone si grid diferit pe desktop, mediu si mobil: zonele HTML sunt in `index.html` (line 53), iar gridul si breakpoint-urile sunt in `resurse/css/stil.css` (line 202), `resurse/css/stil.css` (line 601) si `resurse/css/stil.css` (line 631).
- Design rudimentar cu spacing lateral, gap, padding unitar si delimitarea zonelor prin fundal, border, colturi rotunjite si umbre: implementarea este in `resurse/css/stil.css` (line 8), `resurse/css/stil.css` (line 61), `resurse/css/stil.css` (line 202) si `resurse/css/stil.css` (line 214).
- Media cu latimi responsive, maxime si minime: regulile generale pentru imagini, iframe-uri si object-uri sunt in `resurse/css/stil.css` (line 46) si `resurse/css/stil.css` (line 272).
- Font extern prin Google API si iconuri Font Awesome: importurile sunt in `index.html` (line 12), iconul static este in `index.html` (line 56), iconul animat este in `index.html` (line 206), iar animatia este in `resurse/css/stil.css` (line 501).
- Stilizare tabel individuala cu `caption` jos, alternanta pe coloane, hover pe rand, colturi rotunjite si container cu scroll orizontal: HTML-ul este in `index.html` (line 116), iar CSS-ul in `resurse/css/stil.css` (line 295).
- Stilizare taburi cu iframe, butoane si flexbox: HTML-ul pentru container, taburi si iframe este in `index.html` (line 254), iar stilurile in `resurse/css/stil.css` (line 416).
- Corectie de utilizare pentru dropdown-ul din `Acasa`, astfel incat submenu-ul sa nu se inchida imediat la trecerea cu cursorul: este in `resurse/css/stil.css` (line 150) si `resurse/css/stil.css` (line 175).
- `link-top` individual cu forma rotunjita sus, tooltip, opacitate si efect la hover: HTML-ul este in `index.html` (line 373), iar CSS-ul in `resurse/css/stil.css` (line 522).
- Bonus reset CSS separat: resetarea este in `resurse/css/reset.css` (line 1).
- Bonus MathML stilizat: formula este in `index.html` (line 293), iar stilizarea in `resurse/css/stil.css` (line 469).
